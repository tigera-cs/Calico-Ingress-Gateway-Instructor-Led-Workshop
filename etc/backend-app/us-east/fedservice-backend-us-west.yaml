apiVersion: v1
kind: Service
metadata:
  name: federated-backend-us-west
  annotations:
    federation.tigera.io/serviceSelector: app == "backend-us-west"
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
  type: ClusterIP